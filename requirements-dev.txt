# Python dev / test dependencies. Install with:
#   pip install -r requirements-dev.txt
pytest==9.0.3
pytest-asyncio>=0.23
moto[dynamodb,s3,stepfunctions,lambda,bedrock-agentcore]>=5.0
freezegun>=1.5
cdk-nag>=2.0
